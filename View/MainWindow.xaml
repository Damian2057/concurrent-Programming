<Window
    Height="404"
    Title="MainWindow"
    Width="718"
    mc:Ignorable="d"
    x:Class="Presentation.View.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:v="clr-namespace:Presentation.ViewModel;assembly=ViewModel">
    <Window.DataContext>
        <v:ViewModelClass/>
    </Window.DataContext>
    <Grid>
        <ItemsControl
            ItemsSource="{Binding GetBalls}"
            Margin="0,0,136,0">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <Canvas>
                        <Canvas.Background>
                            <RadialGradientBrush>
                                <GradientStop Color="#E6A4B4" Offset="1"/>
                                <GradientStop Color="#F5EEE6" Offset="0.007"/>
                            </RadialGradientBrush>
                        </Canvas.Background>
                    </Canvas>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemContainerStyle>
                <Style TargetType="ContentPresenter">
                    <Setter Property="Canvas.Left" Value="{Binding XPos}" />
                    <Setter Property="Canvas.Top" Value="{Binding YPos}" />
                </Style>
            </ItemsControl.ItemContainerStyle>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Path Fill="#F6F7D7">
                        <Path.Data>
                            <EllipseGeometry RadiusX="{Binding Radius}" RadiusY="{Binding Radius}" />
                        </Path.Data>
                    </Path>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
        
        <Rectangle HorizontalAlignment="Left" Height="388" Margin="582,0,0,0" Stroke="Black" VerticalAlignment="Center" Width="136">
            <Rectangle.Fill>
                <LinearGradientBrush StartPoint="0,1" EndPoint="1,1">
                    <GradientStop Color="#F3D7CA" Offset="0.0" />
                    <GradientStop Color="#F3D7CA" Offset="1.0" />
                </LinearGradientBrush>
            </Rectangle.Fill>
        </Rectangle>
        <TextBox IsEnabled="{Binding Button}" Text="{Binding NumberOfBalls}" HorizontalAlignment="Left" Margin="588,50,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Width="120"/>
        <Label Content="Enter number of balls:" HorizontalAlignment="Center" Height="23" Margin="588,22,0,0" VerticalAlignment="Top" Width="120" FontSize="10" FontFamily="Bahnschrift SemiBold"/>
        <Button Content="Start" Command="{Binding _start}" HorizontalAlignment="Left" Background="White"  Height="29" Margin="588,73,0,0" VerticalAlignment="Top" Width="120" FontFamily="Bahnschrift SemiBold"/>
        <Button Content="Stop" Command="{Binding _stop}" HorizontalAlignment="Left" Height="29" Background="#C86B85" Margin="588,111,0,0" VerticalAlignment="Top" Width="120" FontFamily="Bahnschrift SemiBold"/>

    </Grid>
</Window>